import { StyleSheet } from "react-native"

const primaryColor = "purple"

const screenStackStyles = {
  headerStyle: {
    backgroundColor: primaryColor,
  },
  headerTintColor: '#fff',
  headerTitleStyle: {
    fontWeight: 'bold',
  }
}

export default StyleSheet.create({
 
})

export {screenStackStyles}
